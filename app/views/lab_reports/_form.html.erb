<%= form_with(model: lab_report, local: true) do |form| %>
  <% if lab_report.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(lab_report.errors.count, "error") %> prohibited this lab report from being saved:</h2>

      <ul>
        <% lab_report.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title, placeholder: "Title" %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_area :description, placeholder: "This description from you report"%>
  </div>

  <div class="field">
    <%= form.label :grade %>
    <%= form.text_field :grade, placeholder: "Format: A B C D E F FX" %>
  </div>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.text_field :user_id, placeholder: "User ID" %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
